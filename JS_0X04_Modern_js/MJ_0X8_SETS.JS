// SETS
// a collection iof a set of values and dont have dupilicate
const sets = new Set(["hi", "hello", "name", "name"]);
console.log(sets);
//all of the duplicates are gone
console.log(sets.size);
